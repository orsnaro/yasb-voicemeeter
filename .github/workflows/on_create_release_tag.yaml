name: Create Release & Update README

on:
  push:
    tags:
      - 'v*'
      - 'V*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Important to see history for notes

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # TODO
      # - name: Update README with Release Info
      #   run: |
      #     # Create the content we want to inject
      #     RELEASE_TEXT="### [${{ github.ref_name }}](https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }})\n\nReleased on $(date +'%Y-%m-%d')"
          
      #     # Use perl to replace the block between our markers
      #     perl -i -0777 -pe "s/().*()/\$1\n\n$RELEASE_TEXT\n\n\$2/s" README.md

      # - name: Commit and Push README changes
      #   run: |
      #     git config --global user.name "github-actions[bot]"
      #     git config --global user.email "github-actions[bot]@users.noreply.github.com"
      #     git add README.md
      #     git commit -m "docs: update README with release ${{ github.ref_name }}"
      #     git push origin main
